// Placeholder SEO component removed. Previously used react-helmet to inject meta tags.
// The project no longer uses dynamic head management. Keep a noop export to avoid
// accidental import runtime errors until all references are purged.

const SEO = () => null;

export default SEO;